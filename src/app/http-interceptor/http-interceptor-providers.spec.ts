import { HttpInterceptorProviders } from './http-interceptor-providers';

describe('HttpInterceptorProviders', () => {
  it('should create an instance', () => {
    expect(new HttpInterceptorProviders()).toBeTruthy();
  });
});
